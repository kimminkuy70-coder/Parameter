# 최소 실험 설계 — kV·R(·p) → void 측정 정확도 모델링 (총 ~15런)

목표: 3개 자재(20/80/182µm)로 **"이 kV·R 설정이면 void%가 ±ε 안에서 정확하다"를 예측하는 모델** 완성.
원칙: 이미 가진 데이터(60kV 대량)를 최대한 재사용, 새 런은 미지수 1개당 1~2런만.

## 0. 모델이 요구하는 미지수 (실험을 역산하는 출발점)

```
모델:  Q_dia(kV) = 1 + s·(kV − kV*)     … 자재별 직선 (s=기울기, kV*=최적점)
       void%오차(N) ≈ 2/N + 노이즈항      … N = D√p/R (자재 무관 공통 곡선)
       유효조건: 매칭 창 안 (창 가장자리 위치)
```
| 미지수 | 지금 상태 | 필요한 새 데이터 |
|--------|-----------|------------------|
| s (기울기) — 자재별 | TB500M만 실측(−3%p/kV) | 80µm·182µm 각 **1런** (60kV는 기존 66·40건 재사용) |
| kV* — 자재별 | 80µm=60 확정, 182µm≈60.7, 20µm 미지 | 20µm만 탐색 필요 (**2~3런**) |
| N 곡선 (오차 vs N) | 이론(2/N)뿐, 실측 없음 | void 있는 웨이퍼 1장 R 스윕 (**5런+반복1**) |
| kV×R 상호작용 | TEST세트가 존재를 시사(R0.2에서 Q 35%) | R스윕 각 런에서 Q_dia가 공짜로 나옴 → 별도 런 불요 |
| p (void 임계%) | – | **실험 불요**: p는 소프트웨어 임계값. 실제 void는 크기가 제각각이라 R 스윕 한 번에 여러 N이 동시에 측정됨 → p 차원은 공짜로 커버 |

## 1. 실험 A — kV 기울기·최적점 (자재별 2점 확보, 총 5~6런)

기존 60kV 데이터가 1점이므로 **자재당 1런만 추가**하면 직선이 그려진다.

| Run | 자재 | kV | R | 목적 |
|-----|------|----|----|------|
| A1 | 80µm | **62** | 0.6 | 기울기 s₈₀ (60kV 66건과 연결) |
| A2 | 182µm | **62** | 1.0 | 기울기 s₁₈₂ (60kV 40건과 연결) |
| A3 | 20µm | **55** | 0.2 | 매칭 절벽 탐색 ① |
| A4 | 20µm | **50** | 0.2 | 절벽 탐색 ② (A3 성공 시 57로 대체) |
| A5 | 20µm | (매칭된 kV)±2 | 0.2 | 기울기 s₂₀ |

- 판정은 자동: 매칭 여부 + Q_dia(측정직경/nominal).
- A3~A5는 이분탐색이라 결과에 따라 3런 안에 끝남. 장비 kV 하한 확인 선행.

## 2. 실험 B — N 곡선 실측 (void 있는 웨이퍼 1장, 총 7런)

**차량(vehicle): TB500M LotID 126173086 slot 17** — void 실존 확인된 유일한 웨이퍼
(RUN12에서 area 기준 void 다수, TEST5에서 10개 검출).

| Run | kV | R | 목적 |
|-----|----|----|------|
| B0 | 58→57 트림(1~2런) | **0.2** | R=0.2에서 Q_dia≈100% 되는 kV 찾기 → **기준(reference) 확보** |
| B1 | B0의 kV 고정 | 0.2 | 기준 void% 측정 (B0와 병합 가능) |
| B2 | 〃 | 0.6 | N 곡선 점 |
| B3 | 〃 | 1.0 | 〃 |
| B4 | 〃 | 1.6 | 〃 |
| B5 | 〃 | 1.0 반복 | 재현성(노이즈 바닥) |

- **분석**: 같은 void를 R별로 추적 → void%(R) vs 기준값의 편차 = 실측 오차곡선.
  void마다 크기가 달라 **한 스윕으로 N 여러 값이 동시에 측정**됨 (2/N 이론과 대조 → 실제 N_target 확정).
- **B0가 중요한 이유**: TEST세트에서 R=0.2가 직경을 35%로 왜곡했음 — kV를 함께 맞추지 않으면
  기준 자체가 오염됨. B0에서 Q≈100%를 만든 뒤의 void%만 기준으로 인정.
- 각 런의 Q_dia도 자동 기록 → **kV×R 상호작용이 공짜로 측정됨.**

## 3. 실험 C — 모델 검증 (총 3런)

A·B로 모델 완성 후, 각 자재에 **모델이 계산한 (kV*, R*)** 로 1런씩:

| Run | 자재 | 조건 | 합격 기준 |
|-----|------|------|-----------|
| C1 | 20µm | 모델 예측값 | 매칭 100% + Q_dia 97~103% + void% 재현 |
| C2 | 80µm | 〃 (R은 0.89로 완화 예측) | 〃 + tact 단축 확인 |
| C3 | 182µm | 〃 (R 2.0 완화 예측) | 〃 + tact 단축 확인 |

C가 통과되면 모델은 "예측 → 1런 검증"으로 새 자재에 이관 가능한 상태가 된다.

## 4. 총계와 원칙

- **총 15~16런** (A 5~6 + B 7 + C 3). 기존 33런 계획 대비 절반 이하.
- 최소화 장치: ① 60kV 대량 데이터를 각 직선의 1점으로 재사용 ② p는 실험 없이 R스윕에 흡수
  ③ kV×R 상호작용은 각 런의 Q_dia 부산물로 측정 ④ tact도 각 런 타임스탬프로 공짜 측정.
- 같은 자재는 **같은 웨이퍼**로 고정(웨이퍼간 편차 제거). 런당 die 5~6개 유지.
- 실행 순서: **A1·A2(하루) → A3~A5 → B → C.**

## 5. 완성 시 얻는 것
```
입력(자재): 범프 직경 D, void 임계 p    ← 도면/스펙
모델 출력:  kV* (자재 기울기·오프셋), R* = D√p/N_target (N_target 실측 확정)
운용:      새 자재 = kV 2점 트림만으로 셋업 (시행착오 15런 → 2런)
```
