# kV 증거 재정리 (정정) + R을 정할 때 N이 필요한 이유

> 이 문서는 INSPECTION_PARAM_DERIVATION.md §3의 kV 멱법칙 유추(20µm→38kV, 182µm→79kV)를
> **폐기·정정**한다. 실험 데이터를 다시 정리한 결과 그 유추는 데이터와 정면으로 배치된다.

---

## 1. kV에 관한 실험 증거 전부 (3개 독립 계열)

### 증거 1 — TB500M RUN 세트: R=1.0 고정, kV만 변화 (가장 깨끗한 통제 비교)
| kV | 측정직경 (nominal 26µm) | Q_dia |
|----|--------------------------|-------|
| 55 | 매칭실패 (측정 불가) | – |
| 60 | 24.0µm (4개 런 동일) | 92% |
| 62.5 | 22.0µm | 85% |
| 65 | 매칭실패 | – |

→ **기울기: kV +1당 직경 약 −2.8%p.** kV를 올리면 범프가 점점 "말라 보임".

### 증거 2 — TB500M TEST 세트: R=0.2 고정, kV만 변화 (절대값은 과소측정 상태지만 추세는 유효)
| kV | 측정직경 |
|----|----------|
| 58 | 12.0µm |
| 59 | 10.0µm |
| 60 | 9.0µm |
| 63.5 | 매칭실패 |

→ 증거 1과 **동일한 단조 추세** (kV↑ → 직경↓). 독립 재현.

### 증거 3 — NXP 3개 자재: kV=60 고정, 범프 크기만 다름
| 범프 | 결과 @60kV |
|------|-----------|
| 20µm | 전멸 (매칭실패) |
| 80µm | **Q=100%** (80.3µm) |
| 182µm | **Q=102%** (185µm) |

## 2. 재해석 — kV의 올바른 그림

**(a) kV는 "측정직경 다이얼"이다.** kV↑ → 측정직경↓ (두 계열에서 재현).
물리: kV를 올리면 감쇠(μ)가 줄어 범프 가장자리(경로 짧은 부분)부터 임계 대비 아래로 떨어짐
→ 어두운 영역이 중심으로 수축 → 작게 측정. 내리면 반대로 커져 보임.

**(b) 각 자재의 최적 kV (Q_dia=100% 지점, 기울기 −2.8%/kV로 역산):**
| 자재 | 최적 kV (추정) |
|------|----------------|
| TB500M (26µm, h40.5) | **≈ 57~58** (60에서 Q92% → 아래로 ~2.5kV; 단 55는 매칭실패라 창 가장자리) |
| NXP 80µm | **60.0** (이미 100%) |
| QORVO 182µm | **≈ 60.5~61** (102% → 위로 ~0.7kV) |

**(c) ★ 핵심 정정: 최적 kV의 크기 의존성은 "매우 약하다".**
크기 7배(26→182µm) 차이에 최적 kV 이동은 **~3kV뿐**. 
이전 멱법칙 유추(직경^(1/3): 38→79kV)는 **데이터와 배치 → 폐기**.
이유(정성): kV 창의 중심은 범프보다 **전체 적층(die+기판) 투과**가 좌우하고,
범프 크기는 그 위에서 ±수 kV의 미세 트림만 요구한다.

**(d) kV의 두 얼굴 — 혼동 금지:**
| 효과 | 거동 | 데이터 |
|------|------|--------|
| **매칭(검출) 성립** | 창(window) — 벗어나면 급락(절벽) | TB500M: 55·65 전멸, 창폭 ±2.5kV |
| **직경 정확도** | 창 안에서 연속 다이얼 (−2.8%/kV) | 58~62.5 구간의 Q 변화 |

→ 운용 규칙: **창 안에서, Q_dia=100%가 되도록 1kV 단위로 트림.** 
20µm 전멸은 "매칭 절벽" 케이스 — 방향은 kV↓지만 몇 kV인지는 데이터 없음(하향 스윕 필요).
얇은 범프 대비는 μ∝V⁻³ 근사로 kV×0.9당 +35% 정도 — 절벽 폭이 크면 50kV 이하까지 필요할 수 있음.

## 3. R을 정하는 데 N이 왜 필요한가

**R(µm/px)은 그 자체로는 판단 기준이 없다.** "0.6이 좋은가 1.0이 좋은가?"에 답하려면
"무엇을 몇 픽셀로 볼 것인가"라는 **요구조건**이 있어야 하고, 그게 N이다.

**논리 사슬:**
1. 검사의 최종 출력은 **void 면적%** (예: 8% 이상이면 불량).
2. 우리가 재야 할 가장 작은 대상 = **임계 크기의 void**. 물리 크기는 `d_void = D·√p`
   (예: 80µm 범프의 5% void = 17.9µm).
3. 이미지에서 이 void가 차지하는 픽셀 폭 = `d_void / R` ≡ **N**.
   → **N은 "물리 세계(µm)"와 "이미지 세계(픽셀)"를 잇는 유일한 다리.**
4. 면적 측정 오차는 N이 정한다: 경계 픽셀 ±0.5px의 애매함 때문에
   **면적 상대오차 ≈ 2/N**. (N=7.5 → ±27%, N=30 → ±7%)
5. 시간은 R이 정한다: 타일 수 ∝ 1/R². N을 크게(R 미세) 잡을수록 시간 급증.

**즉: N = 품질 요구(오차 ε)를 픽셀 언어로 번역한 것.**
```
ε (허용 오차) → N = 2/ε (요구조건) → R = d_void/N (설정값)
```
N 없이 R을 고르는 것 = "몇 % 오차까지 허용할지" 정하지 않고 해상도를 감으로 고르는 것.

**실증 (3개 레시피가 그대로 보여줌):**
| 레시피 | void(5%) 크기 | R | **N (void 픽셀)** | 면적오차 2/N | 결과 |
|--------|---------------|---|-------------------|---------------|------|
| 80µm | 17.9µm | 0.6 | **30** | ±7% | 성공 ✅ |
| 182µm | 40.7µm | 1.0 | **41** | ±5% | (품질 여유, 시간 낭비) |
| 20µm | 4.5µm | 0.6 | **7.5** | **±27%** | 5% void가 3.6~6.4%로 요동 — 판정 불능 |

→ 같은 R=0.6이 80µm에선 충분하고 20µm에선 부족한 이유가 **N 하나로 설명**된다.
R은 자재마다 다시 정해야 하지만, **N은 품질 규격이라 자재가 바뀌어도 동일** —
그래서 N을 기준으로 잡으면 `R = D·√p/N` 한 줄로 모든 자재에 이식된다.

## 4. 정정 반영 사항
- INSPECTION_PARAM_DERIVATION.md §3·§4의 kV 예측값(38/79kV)은 본 문서로 대체.
- 20µm/182µm 첫 시도값 수정: **182µm는 60~61kV 유지가 정답에 가깝고**(직경 이미 102%),
  기존에 요청한 "182µm kV 상향 스윕(→82)"은 **소폭(60~63) 확인**으로 축소.
  **20µm 하향 스윕 요청은 유지**(60→57→54→51→48…, 매칭 회복 지점 탐색).
- 남는 미지수: 매칭 창의 위치가 자재(적층)마다 어디냐 — 이것만 자재당 1회 스윕 필요.

---

## 부록 A. kV 추정 과정 상세 (재현 가능하도록 단계별)

**입력 데이터** (이 저장소 원파일):
- `RUN*/**/XRA_VOID_*.csv`의 `Diameter (um)` 중앙값 → 60kV: 24.0µm / 62.5kV: 22.0µm
- 55·65kV는 전 die FailedToMatchCAD → 측정치 없음
- `merged_results` 362건 → 60kV 고정에서 80µm: 80.3µm(100.4%), 182µm: 185µm(102%), 20µm: 측정없음

**단계 1 — Q_dia 변환**: Q = 측정직경/26 → 60kV: 92.3%, 62.5kV: 84.6%

**단계 2 — 기울기**: (84.6−92.3)/(62.5−60) = **−3.1%p/kV ≈ −2.8~−3.1**
교차확인(TEST 세트, R=0.2): 58→12.0(46%), 59→10.0(38%), 60→9.0(35%)
→ −8, −3 %p/kV. 부호 동일, 크기 동차수. (R=0.2 과소측정 상태라 절대값은 참고만)

**단계 3 — Q=100% 지점 선형외삽**:
- TB500M: 60 − (100−92.3)/2.8 ≈ **57.2kV**
- NXP80: 이미 100.4% → **60kV**
- QORVO182: 60 + (102−100)/2.8 ≈ **60.7kV**

**가정(한계) 명시**:
1. 기울기는 TB500M 2점(60, 62.5)에서만 실측 — 다른 자재에 같은 기울기를 가정(근사)
2. 60 아래로의 선형 외삽 가정 — 55에서 매칭 절벽이 있으므로 57.2가 창 안인지는 미확인
3. 결론은 "첫 시도값"이며 1kV 트림 실험으로 확정해야 함

## 부록 B. Power 운용 규칙

- 실측: 60kV에서 3.5/4.0/4.5W 이미지 완전 동일(배경 254.3, 노이즈 4.8, 대비 223) → 이 범위에선 무효과
- SOP 슬라이드5에 4W vs 5W 검출력 예시 존재 → 시험 범위 밖(5W+)에서 효과 가능성은 열어둠
- **규칙**: ① 기본 4W 고정 ② 직경/대비 문제는 절대 W로 만지지 않음(kV의 일) 
  ③ 매우 미세 R + 두꺼운 적층으로 Frame을 올려도 노이즈가 남을 때만 W 한 단계(5W) ↑ 후
  경계 흐림(스팟 확대) 확인 ④ (선택) 2W·6W 1회씩 브래킷 테스트로 안전 구간 지도화

## 부록 C. R–N 운용 규칙 (어떤 N에서 R을 조절하나)

N은 측정해서 반응하는 값이 아니라 **먼저 정하는 목표**다:
```
① 목표 N 정하기:  N_target = 2/ε   (ε=허용 void% 오차. ±10% → N_target=20)
② 현재 N 계산:    N_now = D·√p / R  (자재 D, 판정 p, 현재 R)
③ 비교:
   N_now < N_target  → 픽셀 부족 → R을 줄여라(미세하게)   R_new = D·√p/N_target
   N_now > N_target  → 과잉(시간 낭비) → R을 늘려라(거칠게)  R_new = 동일식
   N_now ≈ N_target  → 유지
```
예 (p=5%, ε=±10% → N_target=20):
| 자재 | N_now | 판정 | R 조치 |
|------|-------|------|--------|
| 20µm, R0.6 | 7.5 | 부족 | R→0.22 (장비최소 0.2) |
| 80µm, R0.6 | 30 | 과잉(여유) | 유지 또는 R→0.89 (2.2배 빠름) |
| 182µm, R1.0 | 41 | 과잉 | R→2.0 (4배 빠름) |

## 부록 D. 다른 자재(merged 362건) Q 분포 분석

| 자재 | n | Q 중앙값 | 분포 (Q1~Q3) | 이상치 |
|------|---|----------|--------------|--------|
| NXP 80µm | 66 | **100.4%** | 100~101% (최소 98, 최대 102) | 0건 — 매우 안정 |
| QORVO 182µm | 40 | **101.7%** | 101~102% (본체는 좁음) | **6건 39~67%** (전부 mixed) |
| ST 20µm | 0 | 측정 없음 (전멸) | – | – |

- NXP 80µm: 66건이 98~102%에 밀집 → 60kV가 이 자재의 정답임을 강하게 지지.
- QORVO 182µm: 본체는 101~102%로 일관(60kV 근사 정답) — 단 특정 웨이퍼들(UWT/UWTRE/uws/wsc/wst)
  6건만 39~67%로 뚝 떨어짐 → kV 문제가 아니라 **특정 웨이퍼/로트의 국소 이상**(정렬·자재편차)일 가능성.
  → "182µm는 kV를 크게 올려야 한다"는 가설을 재차 반박하는 분포.
- 한계: merged 자재들은 전부 60kV 단일점이라 **자재별 기울기(−%p/kV)는 측정 불가** —
  TB500M 기울기를 이식한 것이며, NXP/QORVO에 60±2kV 트림 1회씩이면 자재별 기울기도 확정됨.

> **[정정 참조]** 부록 B의 "3.5~4.5W 무효과" 판정은 포화된 배경/전역 통계 기준이었음 —
> W가 실제 작동하는 곳(범프 내부, 광자 부족 영역)의 판정 기준과 스윕 설계는
> **DOE_MASTER_PLAN.md 부록 W** 참조. 두꺼운 범프(158µm급)에서는 W가 유효 변수.
